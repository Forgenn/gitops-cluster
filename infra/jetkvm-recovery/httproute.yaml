apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jetkvm-recovery-route
  namespace: jetkvm-recovery
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy-gateway
      namespace: envoy-gateway
      sectionName: https
  hostnames:
    - "jetkvm-recovery.monederobox.dev"
  rules:
    - filters:
        # JetKVM's app 500s on an unexpected Host header; make it look like
        # direct LAN access (Host: 192.168.1.172), which we confirmed works.
        - type: URLRewrite
          urlRewrite:
            hostname: "192.168.1.172"
      backendRefs:
        - group: ""
          kind: Service
          name: jetkvm
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: jetkvm-recovery-allow
  namespace: jetkvm-recovery
spec:
  targetRefs:
    - group: "gateway.networking.k8s.io"
      kind: HTTPRoute
      name: jetkvm-recovery-route
  authorization:
    defaultAction: Deny
    rules:
      - action: Allow
        principal:
          clientCIDRs:
            - 80.233.0.0/16
