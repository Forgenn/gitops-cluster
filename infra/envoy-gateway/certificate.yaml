apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: envoy-tls
  namespace: envoy-gateway
spec:
  secretName: envoy-tls
  issuerRef:
    name: letsencrypt-porkbun
    kind: ClusterIssuer
  dnsNames:
    - "*.monederobox.dev"
    - monederobox.dev
