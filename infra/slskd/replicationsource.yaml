apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: slskd-app
spec:
  sourcePVC: slskd-app
  trigger:
    schedule: "0 2 * * *"
  restic:
    copyMethod: Direct
    pruneIntervalDays: 7
    repository: slskd-app-restic-config
    retain:
      daily: 7
      within: 3d
