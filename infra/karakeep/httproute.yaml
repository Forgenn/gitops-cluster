apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: karakeep-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy-gateway
      namespace: envoy-gateway
      sectionName: https
  hostnames:
    - "karakeep.monederobox.dev"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: karakeep
          port: 3000
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
