# F4 / P-05: see backend/pdb.yaml for rationale.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: loomie-frontend
  namespace: loomie
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: loomie-frontend
