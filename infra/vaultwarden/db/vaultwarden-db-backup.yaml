apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: vaultwarden-cnpg-cluster-backup
  namespace: vaultwarden
spec:
  immediate: false
  schedule: "0 0 8 * * *"
  backupOwnerReference: self
  cluster:
    name: vaultwarden-cnpg-cluster
