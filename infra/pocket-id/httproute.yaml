apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pocket-id-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy-gateway
      namespace: envoy-gateway
      sectionName: https
  hostnames:
    - "pocket-id.monederobox.dev"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: pocket-id
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
